{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: FastAPI",
            "type": "python",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "main:app",
                "--host", "0.0.0.0",  // Bind to all network interfaces
                "--port", "8001",    // Set the port to 8001
                "--reload"
            ],
            "jinja": true,
            "justMyCode": true
        }
    ]
}
